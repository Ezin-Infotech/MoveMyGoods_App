/* ONBOARDING */

enum LoginStatus {
  initial,
  loading,
  loaded,
  error,
}

enum ChangeProfilestatus {
  initial,
  loading,
  loaded,
  error,
}

enum GetAllProfilestatus {
  initial,
  loading,
  loaded,
  error,
}

enum CreatePostStatus {
  initial,
  loading,
  loaded,
  error,
}

enum GetAllBookingCountStatus {
  initial,
  loading,
  loaded,
  error,
}
